# Helper - Приложение для управления шаблонами текстов

Современное GTK приложение на Python с интуитивным интерфейсом для управления шаблонами текстов.

## ✨ Возможности

- 📝 Управление шаблонами текстов
- 🎯 Категоризация по типам (Клиенты, Коллеги)
- 📋 Быстрое копирование текстов
- 🌙 Темная тема оформления
- ⚙️ Отсутствие рамок окна (frameless design)
- 🔄 **Автоматическое обновление приложения**

## 📋 Требования

- Python 3.9+
- Windows/macOS/Linux

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск приложения

**Способ 1: Запуск как Python скрипт**
```bash
python main.py
```

**Способ 2: Запуск .exe файла (только Windows)**
1. Соберите приложение (см. ниже)
2. Запустите `Helper.exe`

## 🔨 Сборка .exe файлов (Windows)

### Автоматическая сборка

```bash
python build_exe.py
```

Скрипт создаст два файла в папке `dist/`:
- **Helper.exe** (~31 MB) - основное приложение
- **updater.exe** (~8 MB) - программа обновления

### Ручная сборка

```bash
# Helper.exe
python -m PyInstaller --onefile --windowed --name Helper --add-data "version.json;." main.py -y

# updater.exe
python -m PyInstaller --onefile --console --name updater updater.py -y
```

## 🔄 Система автоматического обновления

### Как это работает

1. **При запуске** Helper.exe проверяет версию на GitHub
2. **Если доступна новая версия** - показывает диалог с предложением обновиться
3. **При согласии:**
   - Скачивает новую версию как `Helper_update.exe`
   - Закрывает приложение
   - Запускает `updater.exe`
4. **updater.exe** заменяет старый `Helper.exe` на новый
5. **Запускает обновленное приложение**

### Создание нового релиза

1. **Обновите версию** в `version.json`:
   ```json
   {
     "version": "1.1.0",
     "download_url": "https://github.com/teja1337/HelperTemplates/releases/download/v1.1.0/Helper.exe"
   }
   ```

2. **Пересоберите приложение:**
   ```bash
   python build_exe.py
   ```

3. **Создайте Release на GitHub:**
   - Перейдите: https://github.com/teja1337/HelperTemplates/releases/new
   - Tag version: `v1.1.0`
   - Release title: `Version 1.1.0`
   - Загрузите `Helper.exe` из папки `dist/`
   - Опубликуйте релиз

4. **Закоммитьте и отправьте version.json:**
   ```bash
   git add version.json
   git commit -m "Update version to 1.1.0"
   git push
   ```

Теперь все пользователи получат уведомление об обновлении!

## 📁 Структура проекта

```
Helper/
├── main.py                      # Главный файл приложения
├── requirements.txt             # Зависимости Python
├── setup.py                     # Конфигурация для setup
├── build_exe.py                 # Скрипт для создания .exe
├── updater.py                   # Программа обновления
├── version.json                 # Информация о версии
├── models/
│   ├── __init__.py
│   └── template_manager.py      # Управление шаблонами
├── views/
│   ├── __init__.py
│   ├── main_window.py           # Главное окно приложения
│   └── template_widgets.py      # Виджеты шаблонов
├── utils/
│   ├── __init__.py
│   ├── clipboard.py             # Работа с буфером обмена
│   └── updater.py               # Модуль автообновления
├── styles/
│   └── theme.py                 # Темы оформления
├── templates_clients.json       # Шаблоны для клиентов
├── templates_colleagues.json    # Шаблоны для коллег
└── dist/
    ├── Helper.exe               # Готовое приложение (после сборки)
    └── updater.exe              # Программа обновления (после сборки)
```

## 📦 Зависимости

- **customtkinter** - Современный GUI фреймворк
- **pillow** - Работа с изображениями
- **requests** - HTTP запросы для проверки обновлений
- **pyinstaller** - Создание исполняемых файлов

## 🐛 Решение проблем

### Приложение не запускается
- Проверьте Python версию: `python --version` (нужна 3.9+)
- Переустановите зависимости: `pip install -r requirements.txt`

### .exe файлы не создаются
- Убедитесь, что PyInstaller установлен: `pip install pyinstaller`
- Запустите `python build_exe.py` от администратора

### Ошибка "module not found"
- Установите все зависимости: `pip install -r requirements.txt`
- Проверьте, что вы в правильной папке проекта

### Автообновление не работает
- Убедитесь, что `updater.exe` находится в той же папке что и `Helper.exe`
- Проверьте подключение к интернету
- Убедитесь, что `version.json` доступен на GitHub

## 📝 Лицензия

MIT License

## 👤 Автор

teja1337

---

Для обновлений посетите: https://github.com/teja1337/HelperTemplates
