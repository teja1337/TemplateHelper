# 🎨 Инструкция по замене иконок Helper

## Текущие иконки:

```
c:\Code\Helper\
├── icon.ico              <- Иконка приложения (в .exe файле)
└── installer_icon.ico    <- Иконка установщика
```

## Как заменить иконку приложения:

### Способ 1: Использовать готовую иконку (.ico файл)

1. **Создайте или найдите иконку** в формате `.ico` (поддерживает множественные размеры)
   - Рекомендуемые размеры: 16x16, 32x32, 48x48, 64x64, 128x128, 256x256 пиксели

2. **Замените файл:**
   - Удалите старый: `c:\Code\Helper\icon.ico`
   - Поместите новый: `c:\Code\Helper\icon.ico`

3. **Пересоберите приложение:**
   ```bash
   python -m PyInstaller --onefile --windowed --name Helper --add-data "version.json;." --icon icon.ico main.py -y --distpath dist --clean
   ```

### Способ 2: Сгенерировать иконку из PNG/JPG

1. **Подготовьте изображение:**
   - Размер: минимум 256x256 пиксели (квадратное)
   - Формат: PNG или JPG
   - Сохраните как: `c:\Code\Helper\my_image.png`

2. **Установите необходимую библиотеку:**
   ```bash
   pip install pillow
   ```

3. **Используйте скрипт для конвертации:**
   ```python
   from PIL import Image
   
   # Открыть изображение
   img = Image.open('my_image.png').convert('RGBA')
   
   # Сохранить как иконку (несколько размеров)
   sizes = [(16, 16), (32, 32), (48, 48), (64, 64), (128, 128), (256, 256)]
   img.save('icon.ico', sizes=sizes)
   ```

4. **Пересоберите приложение** (как в Способе 1, пункт 3)

### Способ 3: Онлайн конвертация

1. Перейдите на: https://icoconvert.com/
2. Загрузите свой PNG/JPG файл
3. Скачайте .ico файл
4. Поместите в `c:\Code\Helper\icon.ico`
5. Пересоберите приложение

## Для установщика

Точно так же:
1. Замените: `c:\Code\Helper\installer_icon.ico`
2. Пересоберите установщик:
   ```bash
   "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss
   ```

## 📋 Чек-лист для завершения:

- [ ] Выбрали/создали новую иконку
- [ ] Заменили файлы .ico
- [ ] Пересобрали Helper.exe
- [ ] Проверили что иконка отображается в приложении и панели задач
- [ ] Пересобрали установщик (если нужна его новая иконка)
- [ ] Создали релиз на GitHub с новой версией

---

**Примечание:** Иконка также появится:
- На иконке файла Helper.exe в проводнике
- В панели задач
- На ярлыке рабочего стола (после переустановки)
- В меню Пуск (после переустановки через установщик)
